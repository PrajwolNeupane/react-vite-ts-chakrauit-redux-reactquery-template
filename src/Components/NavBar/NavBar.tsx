import { FC } from 'react';
import {Heading} from '@chakra-ui/react';

interface Props{
    
}

let NavBar: FC<Props> = ({ }) => {
    return (
        <>
        <Heading>NavBar</Heading>
        </>
    )
}

export default NavBar;